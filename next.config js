/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',

  images: {
    unoptimized: true,
  },
  
  reactStrictMode: true,
  mode: strict,
  use: strict
}

module.exports = nextConfig

export default eslintConfig